Algorithmic Trading: Machine Learning in Complex Adaptive Markets
---
This repository stores the python code required in order to build a system which evaluates the performance of the machine learning algorithm (random forest) according to the **classification accuracy** of each model using **closing prices**.
